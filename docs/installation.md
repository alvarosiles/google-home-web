# Instalación

> Este documento se completará cuando el proyecto tenga dependencias y scripts de build/dev definidos. Por ahora el repositorio contiene únicamente la estructura de carpetas y archivos base.
