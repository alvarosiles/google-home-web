# server

Backend de la aplicación.

- `src/routes/` — definición de endpoints (sin implementar).
- `src/controllers/` — manejo de peticiones (sin implementar).
- `src/services/` — lógica de negocio del servidor (sin implementar).
- `src/config/` — configuración del servidor.

Ver [docs/api.md](../docs/api.md).
